name: Claude Code

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

jobs:
  claude-review:
    uses: aipoweredmarketer/claude/.github/workflows/claude-pr-review.yml@main
    secrets: inherit